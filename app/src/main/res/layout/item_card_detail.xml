<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    >

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cv_card_detail"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_margin="8dp"
        app:cardCornerRadius="18dp"
        app:cardElevation="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/iv_card_detail"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="center"
            android:layout_margin="4dp"
            tools:background="@drawable/ic_star"
            tools:ignore="ContentDescription" />


    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:id="@+id/tv_card_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:textColor="@color/slate_gray"
        android:textSize="12sp"
        app:layout_constraintStart_toEndOf="@id/cv_card_detail"
        app:layout_constraintTop_toTopOf="@id/cv_card_detail"
        tools:text="Nilai" />

    <TextView
        android:id="@+id/tv_card_desc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="8dp"
        android:textColor="@color/limed_spruce"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/cv_card_detail"
        app:layout_constraintStart_toEndOf="@id/cv_card_detail"
        app:layout_constraintTop_toBottomOf="@id/tv_card_title"
        tools:text="4.8 (3.2k)" />


</androidx.constraintlayout.widget.ConstraintLayout>
